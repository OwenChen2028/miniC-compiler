# Additional Optimizer Tests

## Cases

These source files and LLVM IR files contain additional local and global
optimization cases.

## Files

- `local_opt.c` and `local_opt.ll` cover local optimization.
- `global_opt.c` and `global_opt.ll` cover global optimization.
- `no_opt.c` and `no_opt.ll` contain an unchanged case.
